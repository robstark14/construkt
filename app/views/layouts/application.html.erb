<!DOCTYPE html>
<html>
  <head>
    <title>Construkt</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body class="w-full h-screen">
    <nav class="w-full h-[50px] text-white p-2 dark:bg-gray-800 flex justify-between border-b border-gray-900">
      <div class = "text-xl font-bold text-shadow text-yellow"><%= link_to "Construkt", root_path %></div>
      <% if current_user %>
        <% if current_user.role == 'Admin' %>
          <p class = "text-l text-shadow text-yellow">Hi admin <%=current_user.first_name%> <%=current_user.last_name%>!</p>
        <%else%>
          <p class = "text-l text-shadow text-yellow">Hi, <%=current_user.first_name%> <%=current_user.last_name%>!</p>
      <%end%>
        
        <%= link_to "Logout", destroy_user_session_path, method: :delete %>
      <%end%>
    </nav>


    <p class="notice"><%= notice %></p> 
    <p class="alert"><%= alert %></p> 
    
    <%= yield %>
  </body>
</html>
